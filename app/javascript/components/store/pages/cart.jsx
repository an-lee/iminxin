import React from "react"
import PropTypes from "prop-types"

class Cart extends React.Component {
  state = {
  };

  render() {
    return (
      <div>
        Cart
      </div>
    );
  }
}

export default Cart;
